mutation ResetPassword(
	$token: String!
	$newPassword: String!
	$confirmPassword: String!
) {
	resetPassword(
		token: $token
		newPassword: $newPassword
		confirmPassword: $confirmPassword
	) {
		...DefaultUserResponse
	}
}
