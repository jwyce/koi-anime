query UserAnimeList($options: ListFilterInput!) {
	userList(options: $options) {
		items {
			resourceSlug
			status
			currentEpisode
			anime {
				...DefaultAnime
				episodeCount
			}
		}
		hasMore
		nextCursor
	}
}
