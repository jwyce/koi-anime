query UserMangaList($options: ListFilterInput!) {
	userList(options: $options) {
		items {
			id
			resourceSlug
			status
			currentEpisode
			manga {
				...DefaultManga
				chapterCount
			}
		}
		hasMore
		nextCursor
	}
}
